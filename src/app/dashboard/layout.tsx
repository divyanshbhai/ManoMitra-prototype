// This layout is intentionally left blank to allow different dashboard layouts.
// See src/app/dashboard/student/layout.tsx and src/app/dashboard/employee/layout.tsx

export default function DashboardLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
